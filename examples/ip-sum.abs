# Simple program that fetches your IP and sums it up
ip = $(curl -s 'https://api.ipify.org?format=json')

if ip.ok {
    total = ip.json()["ip"].split(".").map(int).sum()

    if total > 100 {
        echo("The sum of [%s] is %s.", ip, total)
    }
}